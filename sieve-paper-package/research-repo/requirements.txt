numpy>=1.26
matplotlib>=3.9
datasets>=2.20
pyarrow>=15
pypdf>=4
# measured GPT-2 perplexity-gate baseline (scripts/run_gpt2_gate.py)
transformers>=4.40
# torch: CPU-only wheel is sufficient, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
